import React, { ReactElement } from "react";

import Header from "../../components/Header";

const RegistryContainer = ():ReactElement => {

  return (
    <div className="container">
      <Header title="Registro"/>
    </div>
  );
};

export default RegistryContainer;
